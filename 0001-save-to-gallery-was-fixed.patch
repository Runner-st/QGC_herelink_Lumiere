From d149ce36d6ee0bd4574df5f927a4eda7b693072f Mon Sep 17 00:00:00 2001
From: dick <dick@intheass.com>
Date: Thu, 12 Mar 2026 10:45:36 +0000
Subject: [PATCH] save to gallery was fixed

---
 custom/custom.pri                     |  2 +-
 src/Settings/AppSettings.cc           |  5 ++-
 src/VideoReceiver/GstVideoReceiver.cc | 62 ++++++++++++++++++++++++---
 src/VideoReceiver/GstVideoReceiver.h  |  3 ++
 4 files changed, 64 insertions(+), 8 deletions(-)

diff --git a/custom/custom.pri b/custom/custom.pri
index 557e897bf..8110738fb 100644
--- a/custom/custom.pri
+++ b/custom/custom.pri
@@ -20,7 +20,7 @@ win32 {
 } else {
     CUSTOM_QGC_VER_BUILD = $$system("echo $(($$CUSTOM_QGC_VER_BUILD - $$CUSTOM_QGC_VER_FIRST_BUILD))")
 }
-CUSTOM_QGC_VERSION = $${CUSTOM_QGC_VER_MAJOR}.$${CUSTOM_QGC_VER_MINOR}.$${CUSTOM_QGC_VER_PATCH}.1101a
+CUSTOM_QGC_VERSION = $${CUSTOM_QGC_VER_MAJOR}.$${CUSTOM_QGC_VER_MINOR}.$${CUSTOM_QGC_VER_PATCH}.1103
 
 DEFINES -= APP_VERSION_STR=\"\\\"$$APP_VERSION_STR\\\"\"
 DEFINES += APP_VERSION_STR=\"\\\"$$CUSTOM_QGC_VERSION\\\"\"
diff --git a/src/Settings/AppSettings.cc b/src/Settings/AppSettings.cc
index c9cb1b6e6..388179321 100644
--- a/src/Settings/AppSettings.cc
+++ b/src/Settings/AppSettings.cc
@@ -297,11 +297,12 @@ QString AppSettings::videoSavePath(void)
     //     QDir dir(path);
     //     return dir.filePath(videoDirectory);
     // }
-    QString path = "/storage/sdcard0/DCIM";
+    QString path = "/storage/emulated/0/DCIM";
 
-    if (!path.isEmpty() && QDir(path).exists()) {
+    if (QDir(path).exists()) {
         return path;
     }
+
     return QString();
 }
 
diff --git a/src/VideoReceiver/GstVideoReceiver.cc b/src/VideoReceiver/GstVideoReceiver.cc
index 08e63a1bd..4ef049f11 100644
--- a/src/VideoReceiver/GstVideoReceiver.cc
+++ b/src/VideoReceiver/GstVideoReceiver.cc
@@ -569,6 +569,7 @@ GstVideoReceiver::startRecording(const QString& videoFile, FILE_FORMAT format)
     g_object_set(_recorderValve, "drop", FALSE, nullptr);
 
     _recording = true;
+    _lastRecordedFile = videoFile;
     qCDebug(VideoReceiverLog) << "Recording started" << _uri;
     _dispatchSignal([this](){
         emit onStartRecordingComplete(STATUS_OK);
@@ -751,14 +752,14 @@ GstVideoReceiver::_filterParserCaps(GstElement* bin, GstPad* pad, GstElement* el
 static void dumpGObjectProperties(GObject* obj, const char* label)
 {
     if (!obj) {
-        qCCritical(VideoReceiverLog) << label << ": null";
+        qCInfo(VideoReceiverLog) << label << ": null";
         return;
     }
 
     guint nProps = 0;
     GParamSpec** props = g_object_class_list_properties(G_OBJECT_GET_CLASS(obj), &nProps);
 
-    qCCritical(VideoReceiverLog) << "Properties for" << label << ":" << nProps;
+    qCInfo(VideoReceiverLog) << "Properties for" << label << ":" << nProps;
 
     for (guint i = 0; i < nProps; ++i) {
         GParamSpec* pspec = props[i];
@@ -838,7 +839,7 @@ static void dumpGObjectProperties(GObject* obj, const char* label)
             }
         }
 
-        qCCritical(VideoReceiverLog)
+        qCInfo(VideoReceiverLog)
             << " " << propName
             << "[" << g_type_name(valueType) << "]"
             << "=" << out;
@@ -880,7 +881,7 @@ GstVideoReceiver::_makeSource(const QString& uri)
             }
         } else if (isRtsp) {
             if ((source = gst_element_factory_make("rtspsrc", "source")) != nullptr) {
-                g_object_set(static_cast<gpointer>(source), "drop-on-latency", true, "location", qPrintable(uri), "protocols", 0x4, "latency", 100, "udp-reconnect", 1, "timeout", _udpReconnect_us, NULL);
+                g_object_set(static_cast<gpointer>(source), "location", qPrintable(uri), "latency", 17, "udp-reconnect", 1, "timeout", _udpReconnect_us, NULL);
                 dumpGObjectProperties(G_OBJECT(source), "rtspsrc");
             }
         } else if(isUdp264 || isUdp265 || isUdpMPEGTS || isTaisync) {
@@ -1067,7 +1068,7 @@ GstVideoReceiver::_makeFileSink(const QString& videoFile, FILE_FORMAT format)
             break;
         }
 
-        parser = gst_element_factory_make("h265parse", nullptr);
+        parser = gst_element_factory_make("h264parse", nullptr);
         if (!parser) {
             qCCritical(VideoReceiverLog) << "parser create failed";
             break;
@@ -1457,6 +1458,10 @@ GstVideoReceiver::_shutdownRecordingBranch(void)
     gst_bin_remove(GST_BIN(_pipeline), _fileSink);
     gst_element_set_state(_fileSink, GST_STATE_NULL);
     gst_object_unref(_fileSink);
+
+    if (QFile::exists(_lastRecordedFile)) {
+        _scanMedia(_lastRecordedFile);
+    }
     _fileSink = nullptr;
 
     _removingRecorder = false;
@@ -1752,3 +1757,50 @@ GstVideoReceiver::_keyframeWatch(GstPad* pad, GstPadProbeInfo* info, gpointer us
 
     return GST_PAD_PROBE_REMOVE;
 }
+
+
+
+
+
+
+#ifdef Q_OS_ANDROID
+#include <QAndroidJniObject>
+
+void GstVideoReceiver::_scanMedia(const QString& path)
+{
+    QString uri = "file://" + path;
+
+    QAndroidJniObject action = QAndroidJniObject::fromString(
+        "android.intent.action.MEDIA_SCANNER_SCAN_FILE");
+
+    QAndroidJniObject uriObj = QAndroidJniObject::callStaticObjectMethod(
+        "android/net/Uri",
+        "parse",
+        "(Ljava/lang/String;)Landroid/net/Uri;",
+        QAndroidJniObject::fromString(uri).object<jstring>());
+
+    QAndroidJniObject intent(
+        "android/content/Intent",
+        "(Ljava/lang/String;Landroid/net/Uri;)V",
+        action.object<jstring>(),
+        uriObj.object());
+
+    QAndroidJniObject activity =
+        QAndroidJniObject::callStaticObjectMethod(
+            "org/qtproject/qt5/android/QtNative",
+            "activity",
+            "()Landroid/app/Activity;");
+
+    activity.callMethod<void>(
+        "sendBroadcast",
+        "(Landroid/content/Intent;)V",
+        intent.object());
+
+    qCInfo(VideoReceiverLog) << "try to save" << uri;
+}
+#else
+void GstVideoReceiver::_scanMedia(const QString& path)
+{
+    Q_UNUSED(path)
+}
+#endif
\ No newline at end of file
diff --git a/src/VideoReceiver/GstVideoReceiver.h b/src/VideoReceiver/GstVideoReceiver.h
index 44aa4f13b..e2796dd59 100644
--- a/src/VideoReceiver/GstVideoReceiver.h
+++ b/src/VideoReceiver/GstVideoReceiver.h
@@ -116,6 +116,7 @@ protected:
     virtual bool _unlinkBranch(GstElement* from);
     virtual void _shutdownDecodingBranch (void);
     virtual void _shutdownRecordingBranch(void);
+    virtual void _scanMedia(const QString& path);
 
     bool _needDispatch(void);
     void _dispatchSignal(std::function<void()> emitter);
@@ -167,6 +168,8 @@ protected:
     bool                _endOfStream;
 
     static const char*  _kFileMux[FILE_FORMAT_MAX - FILE_FORMAT_MIN];
+
+    QString _lastRecordedFile;
 };
 
 void* createVideoSink(void* widget);
-- 
2.43.0

